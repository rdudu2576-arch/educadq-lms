{
  "name": "educadq-server",
  "version": "1.0.0",
  "main": "dist/server/index.js",
  "scripts": {
    "start": "node dist/server/index.js",
    "dev": "ts-node server/index.ts",
    "build": "tsc && vite build",
    "build:server": "tsc --project tsconfig.server.json",
    "postinstall": "pnpm run build:server"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.1",
    "ts-node": "^10.9.2",
    "tsx": "^3.14.0",
    "typescript": "^5.9.3",
    "vite": "^5.4.20"
  }
}
