{
  "name": "educadq-ead",
  "version": "1.0.1",
  "license": "MIT",
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "dev": "NODE_ENV=development tsx watch server/index.ts",
    "build": "vite build && tsc -p tsconfig.server.json || true",
    "start": "NODE_ENV=production node dist/server/index.js"
  }
}
